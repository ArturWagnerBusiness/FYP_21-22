import { SxProps, Theme } from "@mui/material";

export const S_LoginTitle: SxProps<Theme> = {
  paddingTop: 1,
};
export const S_RowItem: SxProps<Theme> = {
  paddingTop: 1,
};
export const S_SendButton: SxProps<Theme> = {
  paddingTop: 2,
};
